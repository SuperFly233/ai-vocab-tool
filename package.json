{
  "name": "ai-vocab-tool",
  "version": "0.11.18",
  "private": true,
  "type": "module",
  "scripts": {
    "build:pages": "node scripts/build-pages.js",
    "check:contracts": "node scripts/check-contracts.js",
    "check:history": "node scripts/check-history-data.js",
    "check:lookup": "node scripts/check-lookup-tasks.js",
    "check:sync": "node scripts/check-sync-state.js",
    "check:sync-api": "node scripts/check-sync-api.js",
    "check": "node --check app.js && node --check history-data.js && node --check lookup-tasks.js && node --check sync-state.js && node --check api/analyze.js && node --check api/followup.js && node --check api/models.js && node --check api/sync.js && node --check api/test-profile.js && node --check functions/_utils/vercel-adapter.js && npm run check:contracts && npm run check:history && npm run check:lookup && npm run check:sync && npm run check:sync-api"
  }
}
