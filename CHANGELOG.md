# Changelog

## v0.9.26 - 2026-07-13

- 历史备注编辑区新增加粗、列表、引用、代码和表格快捷按钮。
- 按钮会尽量复用当前选区：有选中文字时包裹格式，没有选区时插入可直接填写的模板。
- 插入后会保持焦点并把光标放回备注框，减少手写 Markdown 的操作成本。

## v0.9.25 - 2026-07-13

- 历史筛选区新增 Tag 多选筛选，可按个人标签快速收拢词条。
- 历史列表里的标签可直接点击筛选，并保持原有整条记录点击打开详情的行为。
- 历史筛选摘要、清空和搜索状态会把 Tag 纳入统一判断。

## v0.9.24 - 2026-07-13

- 历史详情编辑页新增标签和备注字段，可给词条补充个人整理信息。
- 历史列表和结果预览会显示已保存的标签与备注，搜索历史时也会纳入匹配。
- 云同步合并历史时会保留两端标签，并通过 `noteUpdatedAt` 正确同步备注修改或清空。

## v0.9.23 - 2026-07-13

- 结果页会根据当前查询和已有历史记录自动显示相关词条，支持单词与包含它的词组互相跳转。
- 相关词条卡片会展示核心释义、词性和方向摘要，点击即可打开对应历史详情。
- 当前词条会被排除在相关列表之外，避免历史详情里出现自我引用。

## v0.9.22 - 2026-07-13

- 设置页新增“字段标签显示”，可在中文名称、英文缩写和中英双语之间切换。
- 历史筛选、历史摘要、词性分组、词条标题区和语体说明会统一使用当前显示偏好。
- API 配置保存与清空逻辑会保留显示偏好，避免非接口设置被误重置。

## v0.9.21 - 2026-07-13

- 历史记录列表新增核心释义、词性、方向和语言摘要，减少只看词条名时的信息盲区。
- 查询结果的例句、译文和义项说明会高亮当前词条与对应义标，更容易对上原词和中文含义。
- 手动清空查询输入时会同步清空当前结果与状态；仍在返回的旧查询不会再污染页面或误保存。
- 追问 Markdown 表格继续使用横向滚动和按列数撑开的宽度，避免手机窄屏多列内容被压扁。

## v0.9.20 - 2026-07-02

- 追问回答新增 GitHub 风格 Markdown 表格渲染，支持表头、分隔行和多行数据。
- 表格单元格继续支持行内 code、链接、粗体、斜体和删除线。
- 表格样式使用横向滚动容器，并按列数撑开最小宽度，避免手机窄屏或多列表格被压扁。

## v0.9.19 - 2026-07-02

- 追问回答新增 `>` 引用块渲染，显示为带左侧强调线的引用版式。
- 追问回答补齐 fenced code block、分隔线、链接、删除线、下划线粗体/斜体等常见 Markdown 规则。
- Markdown 渲染改为逐行解析，减少标题、列表、引用和普通段落之间的误判。

## v0.9.18 - 2026-06-29

- 同源同步兜底会优先读取 Supabase 本地 session token，避免手机端 `getSession()` 刷新会话时再次触发 `Load failed`。
- 保留 SDK `getSession()` 作为后备路径，登录会话有效时可稳定调用 `/api/sync`。
- 用 mock Supabase 跑通 `/api/sync` 的 `select` 和 `upsert` 流程，确认服务端兜底可读写允许的 `ai_vocab_tool_*` key。

## v0.9.17 - 2026-06-29

- 云同步直连 Supabase REST 遇到 `TypeError: Load failed` / `Failed to fetch` 时，会自动改走本站同源 `/api/sync`。
- 新增 Vercel 同步代理，由服务端携带当前用户 token 读写 `public.study_store`，减少手机浏览器跨域网络失败。
- 本机保存和首次登录合并同步都会复用同一套直连优先、同源兜底的读写逻辑。

## v0.9.16 - 2026-06-29

- 拆分云同步读写锁和界面忙碌状态，修复密码登录后“正在登录”状态阻塞首次同步的问题。
- 同步读写增加 `try/catch/finally` 防护，失败后会释放锁并继续处理排队的保存任务。
- 同步错误会提示 `study_store` 表、RLS 权限、登录会话或网络连接等具体排查方向。

## v0.9.15 - 2026-04-29

- 查询结果的义项分析会先按词性分组，再在每个词性下重新编号。
- 词条标题区会从 `headword.basicPartOfSpeech` 和 `senses[].partOfSpeech` 归纳多个词性，不再只显示单个词性。
- AI 输出规则改为允许 `headword.basicPartOfSpeech` 用斜杠记录多个固定词性枚举，修复多词性词条被压成单个词性的结构问题。

## v0.9.14 - 2026-04-29

- 补齐收藏样式使用的金色主题变量，避免高亮和发光效果失效。
- 收藏历史卡片改为金色描边、流光扫过和细星芒背景，保留特殊感但不遮挡右侧操作。
- 收藏按钮改成更明确的金色发光状态，让收藏条目一眼可见。

## v0.9.13 - 2026-04-29

- 修复 API 配置保存后被旧版兼容字段覆盖的问题，新增和编辑的名称、URL、Key、Model 会正确保留。
- API 配置弹窗保存时只写入 profile 列表和当前选中项，避免把旧 active profile 的顶层字段再次带回存储。
- 配置归一化会合并完全重复的 profile，清理此前反复保存产生的默认配置重复项。

## v0.9.12 - 2026-04-28

- 启动时会把首页布局默认值写入本地设置，确保“顶部 / 左右”总有一个按钮处于选中状态。
- 云端同步恢复布局后会重新规范化空值，避免空布局覆盖本地默认。
- 首次初始化默认布局会标记云端同步，确保布局偏好可跨设备保存。

## v0.9.11 - 2026-04-28

- API 配置保存改为 `try/catch` 防护，成功和失败都使用统一 toast 反馈。
- 本地保存成功后立即关闭弹窗，不再停留在“正在保存...”状态。
- 云端同步改为保存后的后台动作，避免同步链路影响配置保存体验。

## v0.9.10 - 2026-04-28

- 收藏历史条目改为更精致的金色高亮、细光边和柔和背景，不再使用粗糙的单边色块样式。
- 修复历史筛选菜单点击选项后被全局点击处理立即关闭的问题，多选时菜单会保持展开。

## v0.9.9 - 2026-04-28

- API 配置弹窗底部按钮改为显式事件绑定，修复“保存配置”点击后没有反应的问题。
- API Key 输入框改为非密码字段，并关闭自动填充/密码管理识别，减少浏览器反复提示保存密码。
- 登录邮箱和登录密码字段补充标准 `autocomplete`，让浏览器更容易区分账号密码和 API 密钥。

## v0.9.8 - 2026-04-28

- API 配置外层只保留当前配置选择，新增和编辑改为弹窗填写名称、API URL、API Key 和 Model。
- 删除配置与清空全部配置逻辑重新整理，最后一组配置也能清回默认空配置。
- 新增模型列表查询代理 `/api/models`，可在配置弹窗里尝试读取 OpenAI-compatible 模型列表。
- 历史筛选改成自制多选菜单，每个分类都支持单选或多选，默认状态用短横显示。

## v0.9.7 - 2026-04-28

- 自定义 API 配置组不再使用原生选择器，改为当前配置卡片和自制切换菜单。
- 新增、保存、删除和恢复默认拆成独立按钮，避免把危险操作混在配置选择里。
- 配置列表会显示每组是否完整，以及当前模型或环境默认模型状态。

## v0.9.6 - 2026-04-28

- 历史筛选改为紧凑下拉控件，避免多选列表占用过大的垂直空间。
- 排序按钮收紧为工具条样式，减少历史页顶部拥挤和错位。
- 收藏历史卡片去掉右侧大星水印，改为轻量左侧高亮和按钮状态，避免遮挡操作区。

## v0.9.5 - 2026-04-28

- 修复重复触发同一条 toast 时，底部进度条不重新开始、自动关闭剩余时长变短的问题。
- 手动关闭 toast 时会同步清理对应计时器，避免后续再次打开时继承旧状态。
- 查询加载态移除圆形 spinner，只保留直线进度条，并优化等待卡片的背景、边框和高光效果。

## v0.9.4 - 2026-04-27

- 设置页移除“当前来源 / 环境变量 / 管理员限制”状态卡，减少对网页配置流程的干扰。
- API 配置组改为集中操作：新增配置、保存修改、删除当前配置。
- “恢复默认”移入 API 配置下拉菜单，让模型/API 管理入口更集中。

## v0.9.3 - 2026-04-27

- 历史筛选改为多选，语言、方向、词性、语体都支持同时选择多个条件。
- 对旧历史中的语言、方向、词性、语体字段做前端归一化，减少 `EN/en/英文`、`英译中/译为中文`、`v./动词` 这类重复筛选项。
- 更新 AI 输出 prompt，要求新生成结果使用固定枚举字段。
- 排序按钮文字和箭头保持居中，筛选控件在桌面和手机端宽度更统一。
- 清空历史的二次确认文案改为更明确的危险操作提示。

## v0.9.2 - 2026-04-27

- 在全部历史视图中，已收藏条目会显示更醒目的收藏卡片背景。
- 收藏条目新增左侧高亮条、轻微暖色光影和淡星标水印。
- 收藏状态也会显示在历史时间信息中，便于快速扫视。

## v0.9.1 - 2026-04-27

- 左上角品牌区新增可点击的网页图标，点击后回到首页。
- 进入主界面、点击品牌图标、点击首页按钮时会自动聚焦搜索框。
- 历史记录重新生成跳回首页时也会自动把焦点放回输入框。

## v0.9.0 - 2026-04-27

- 设置页新增多组 API 配置，可保存多套 API URL、API Key 和 Model。
- 支持在多组配置之间切换当前使用项，查询和追问会使用当前配置组。
- 旧版单组 API 设置会自动迁移为默认配置组。
- 云同步会合并不同设备上的 API 配置组，减少新设备拿不到接口设置的问题。

## v0.8.1 - 2026-04-27

- 右上角通知弹窗新增关闭按钮，自动消失前也可以手动关闭。
- 重做通知弹窗视觉样式，加入状态图标、侧边光条、扫光、进度条和更顺滑的进出动效。
- 通知弹窗改为单条可交互，不影响页面其它区域点击。

## v0.8.0 - 2026-04-27

- 修复云端同步流程，登录后自动合并本机和云端数据，不再依赖弹窗选择。
- 同步范围扩展到历史、收藏、API 设置、主题、布局和运行日志。
- 页面聚焦、从后台回到前台、以及登录状态下定时轮询时会静默拉取云端更新。
- 新增 `PROJECT_CONTEXT.md`，用于新对话快速衔接项目状态、同步策略和后续工作规则。
- README 增加同步策略和项目上下文维护要求。

## v0.7.0 - 2026-04-25

- 新增结果追问功能，可基于当前词条继续向 AI 提问。
- 追问回答以对话卡片显示在结果页下方。
- 追问会保存到对应历史记录中，历史详情里也可以继续查看。
- 追问支持卡片内编辑和删除。

## v0.6.0 - 2026-04-25

- 首页输入框取消伸缩特效，默认完整展示输入条、辅助输入和操作按钮。
- 支持在首页输入框按 Enter 快速查询，Shift+Enter 保留换行空间给后续扩展。
- 查询中显示请求阶段、接口来源和加载动画，减少等待时的黑箱感。
- 历史记录整块可点击进入详情，删除按钮仍保持独立操作。
- 历史详情支持排版、分区 JSON、编辑保存三种视图。
- 结果排版重做层级：义项序号靠左，词性单列，重点内容使用荧光笔底色。
- JSON 视图按 meta、headword、senses、collocations、register、confusions 分区展示。
- 降低全站字重密度，让标题、标签、正文和重点内容有更明确主次。

## v0.5.0 - 2026-04-25

- 新增自定义 API URL、API Key、Model 设置。
- 新增恢复接口默认、恢复出厂设置、运行日志和关于页。
- 历史清空按钮改为垃圾桶图标。

## v0.4.0 - 2026-04-25

- 增强历史记录搜索和排序。
- 优化结果卡片与 JSON 查看体验。
- 加入云端设置同步。
